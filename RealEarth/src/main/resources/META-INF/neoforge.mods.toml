modLoader = "javafml"
loaderVersion = "[4,)"
license = "${mod_license}"
issueTrackerURL = "https://example.invalid/realearth/issues"

[[mods]]
modId = "${mod_id}"
version = "${mod_version}"
displayName = "${mod_name}"
authors = "RealEarth"
description = '''
Real-world Earth terrain, bathymetry, climate, cyclones and biome-accurate fauna for Minecraft.
Reads open geodata (GEBCO, Koppen-Geiger, Natural Earth, WorldClim) and reproduces Earth at 1:1 or 1:2 scale.
'''

[[dependencies.${mod_id}]]
modId = "neoforge"
type = "required"
versionRange = "[21.1.0,)"
ordering = "NONE"
side = "BOTH"

[[dependencies.${mod_id}]]
modId = "minecraft"
type = "required"
versionRange = "[1.21.1,1.22)"
ordering = "NONE"
side = "BOTH"

[[dependencies.${mod_id}]]
modId = "cold_sweat"
type = "optional"
versionRange = "*"
ordering = "AFTER"
side = "BOTH"

# Volumetric clouds. Needs no API from us: it derives its coverage from the vanilla rain and
# thunder levels, which WeatherSync drives per player. Listed so the load order is stable.
[[dependencies.${mod_id}]]
modId = "betterclouds"
type = "optional"
versionRange = "*"
ordering = "AFTER"
side = "CLIENT"

# Far-distance LOD terrain. Also needs no API from us - it calls ChunkGenerator.getBaseHeight,
# which this mod answers straight from the elevation raster.
[[dependencies.${mod_id}]]
modId = "distanthorizons"
type = "optional"
versionRange = "*"
ordering = "AFTER"
side = "CLIENT"

[[dependencies.${mod_id}]]
modId = "create"
type = "optional"
versionRange = "*"
ordering = "AFTER"
side = "BOTH"
